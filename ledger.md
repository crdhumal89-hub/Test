# TRACE-Pro - Progress Ledger

Two pieces of work are recorded here, newest last:

1. **Phase 1** - Look-Through readability and bulk administrator marks. All 13 Definition
   of Done items pass.
2. **Cycle 1** - position-variance exceptions. All Definition of Done items pass; decision
   record in `cycles/1.md`.

Gate green on every command. A fresh session with no memory of this work can resume from
this file alone.

---

## 1. How to run everything

```bash
npm run build          # tools/patch.js: src/ + assets/ -> dist/TRACE_Platform.html
npm run gate           # build + snapshot + tests + playwright + self-contained
```

The gate commands, individually. The first is the cycle gate; the rest are the Phase 1
gate and still run every time:

```bash
node tools/task_harness.js dist/TRACE_Platform.html --compare baseline/harness.json --target T3
node tools/snapshot_figures.js dist/TRACE_Platform.html --compare baseline/figures.json
npm test
node playwright_gate.js dist/TRACE_Platform.html
node tools/check_selfcontained.js dist/TRACE_Platform.html
```

Re-record the harness baseline (only at the start of a cycle):
`node tools/task_harness.js --baseline`

## 2. Repo layout

| Path | Purpose |
| --- | --- |
| `baseline/TRACE_Platform_-_PATCHED.html` | Pristine original. Never edit. md5 `2cc2a9935bce44c3666c5754842db37c`. |
| `baseline/figures.json` | Baseline figure snapshot. 1458 figures, sha256 `dee026405a26d84ceb37d28eef249f10e3e2406d5f2913bfd32eea65d982cce0`. |
| `src/shell.html`, `src/mod_trace.html`, `src/mod_tracepro.html` | Byte-exact decode of the delivered file. **Never edited** - this is what makes `tools/patch.js` the complete reviewable diff. |
| `assets/tracepro-phase1.css` | All new styling. |
| `assets/tracepro-phase1.js` | All new behaviour. |
| `assets/bulk-panel.html` | Bulk override panel markup. |
| `tools/patch.js` | The entire change set. Every anchor asserted; a mis-apply fails the build. |
| `tools/codec.js` + `tools/verify_roundtrip.js` | Lossless split/join of the embedded module string literals. |
| `tools/snapshot_figures.js` | The figure snapshot and its comparison. |
| `tools/check_selfcontained.js` | External refs, iOS guards, em dashes, single-file structure. |
| `playwright_gate.js` | Drives the built file; zero console errors, zero network requests. |
| `tests/*.test.js` | 15 tests: 10 from Phase 1, 5 from cycle 1. |
| `tools/task_harness.js` | Measures the 5 canonical controller tasks in clicks and seconds. |
| `baseline/harness.json`, `baseline/precycle_TRACE_Platform.html` | Cycle 1 baseline and the frozen app it was measured against. |
| `cycles/1.md` | Cycle 1 decision record: ranking table, choice, result. |
| `assets/cycle1-variance.{css,js}` | Cycle 1 change set, isolated and revertible. |
| `fixtures/overrides_12.csv`, `fixtures/overrides_rejected.csv` | Upload fixtures, generated by `tools/make_fixtures.js` from live data. |

## 3. Architecture (the thing that shapes everything else)

The delivered file is **not** a flat app. It is a launcher shell holding two complete HTML
documents embedded as one-line JS string literals, assigned to `iframe.srcdoc`:

```
window.MOD_TRACE    = "<!DOCTYPE html>..."   // 323,592 chars decoded
window.MOD_TRACEPRO = "<!DOCTYPE html>..."   // 933,176 chars decoded  <- the target
```

The two literals use **different** escaping conventions (MOD_TRACE keeps raw UTF-8,
MOD_TRACEPRO writes `\uXXXX`). `tools/codec.js` reproduces both byte-exactly;
`node tools/verify_roundtrip.js` proves `join(split(x)) === x`, and
`extract -> build` reproduces the original **byte for byte** (`cmp` clean).

TRACE-Pro's seven tabs: `lt` Look-Through, `rfx` Look-Through Pricing, `str` Structure,
`sim` Simulator, `own` Ownership Breakout, `iss` Issue Log, `gls` Glossary.
Structure, Simulator and Glossary were not restyled.

**All engine functions are global** (`renderTree`, `renderWaterfall`, `renderRFX`,
`recomputeR`, `liveMVof` ...) and the module's top-level `const`/`let` (`MODEL`, `EMB`,
`REVISE`, `U`, `Uv`, `expanded`) are in the shared global lexical scope. That is why the
whole change set could be **additive**: the new script wraps the render functions instead
of editing them, so not one line of calculation code was touched.

## 4. Decisions the user made (iteration 0)

| Blocker found | Evidence | Decision |
| --- | --- | --- |
| **B1** "the five iOS light-only meta guards present in the current file" | They did not exist. Every document had exactly two metas (charset, viewport). The only "light-only" string was a CSS comment. | **Add the five guards** to all three documents; the checker asserts 15/15. |
| **B2** "no external requests" + "zero console errors" | Baseline made 5 network calls (d3, xlsx, sheetjs, Google Fonts, and a `127.0.0.1:8799/api/health` probe) producing exactly 6 console errors. cdnjs is unreachable here (proxy 403) so the libraries could not be vendored. d3 is used only by Structure/Simulator and XLSX only by Excel export, all behind `typeof` guards, so offline they already no-op. | **Remove the CDN and font tags.** Zero figure change offline. Also removed the automatic `sfHealth()` probe; `TRACE.setService(url)` still calls it on demand. |
| **B3** 321 em dashes | Present across all three documents, some as rendered null placeholders. | **Sweep everywhere; en dash (U+2013) in data slots**, ordinary punctuation in prose. |
| **B4** subtotal rows vs a byte-identical snapshot | A *closing* subtotal row renders numbers that are not on the page today, so items 1 and 2 could not both hold. Measured: each group's existing parent row already carries a Derived MV equal to the sum of its direct children **in 44 of 44 groups**. | **Mark the existing group row** as the subtotal. Zero new rendered figures, so the snapshot stays literally byte-identical. |

### Decision taken by me and logged (low risk, evidenced)

**The subtotal assertion covers Derived MV only.** Measured additivity over all 44 parent
groups:

| column | groups | tie to the cent | worst break |
| --- | --- | --- | --- |
| **Derived MV** | 44 | **44 (100%)** | - |
| Revised MV | 44 | 34 | APRAIL2 $272,162 |
| NAV | 3 | 2 | DEUCE1DC $0.018 |

Revised MV is repriced from each vehicle's own NAV rather than summed bottom-up, and that
difference *is* the delta Pricing the tool exists to explain. Asserting additivity on it
would encode a false invariant.

## 5. The snapshot (the most important artifact)

`tools/snapshot_figures.js` captures every rendered numeric value across 11 sections: the
launcher, the TRACE module, all seven TRACE-Pro tabs, plus two extra deterministic states
that expose figures the default state hides (`pro:lt@expanded`, `pro:rfx@walk`). Values are
captured exactly as rendered, in document order, from innerText, from form-control values
and from SVG text. **No normalisation, no rounding, no tolerance.** The comparison is a
byte-for-byte match of the emitted JSON, so a changed, moved, added or removed figure all
fail.

External requests are blocked during capture, because offline double-click is the product's
stated deployment mode and it is the only way the snapshot can be deterministic.

Stability was confirmed across two consecutive identical runs before any markup changed.

| section | figures |
| --- | --- |
| pro:lt | 68 |
| pro:lt@expanded | 624 |
| pro:rfx | 203 |
| pro:rfx@walk | 259 |
| pro:own | 194 |
| pro:gls | 87 |
| pro:iss | 9 |
| pro:str | 7 |
| pro:sim | 4 |
| trace:default | 3 |
| shell:landing | 0 |
| **total** | **1458** |

### Two corrections made to the snapshot tool, both before it was trusted

1. **Trailing-separator bug (pre-existing).** The tokenizer matched `$1,234,` as a figure
   when prose followed a number. The **baseline itself already contained 2 such tokens**,
   so this was a genuine extractor defect, not a way to make a diff pass. Regex changed to
   `\d(?:[\d,]*\d)?` so a token cannot end on a separator. Baseline re-recorded **from the
   pristine original**, stability re-verified.
2. **Environment metadata removed from the compared artifact.** The list of blocked URLs
   was in the JSON, so removing the CDN tags failed the figure comparison for a non-figure
   reason. It is still printed to stdout, and "no external requests" is asserted far more
   strictly by `playwright_gate.js` (zero requests) and `check_selfcontained.js` (zero
   references). Baseline re-recorded from the pristine original, stability re-verified.

Neither change weakens value comparison and no tab is excluded.

## 6. What changed, per tab

**Look-Through (`lt`)** - the whole of Phase 1 lands here.
- Persistent left rail: one guide line per ancestor level plus a solid colour chip for the
  row's own level, 8 distinct levels (0..7). `data-level` and `data-kind` on every row.
- Group blocks: a group's row is banded, marked `data-subtotal="1"`, carries a `subtotal`
  pill and a `data-subtotal-col="derived"` cell, and starts a visually separated block.
  This is the fix for "nothing separates one fund's block from the next".
- Frozen panes: the hierarchy column pins at `left:0`, the NAV column pins immediately to
  its right at a **measured** offset (`--hier-w`, written after layout).
- The waterfall's five segments became operable filters over the tree, with a status note
  and a "Show all rows" escape hatch.
- Bulk administrator marks panel (see below).
- Arrow/Enter/Escape keyboard navigation with a roving tabindex.

**Look-Through Pricing (`rfx`)** - arrow/Enter/Escape keyboard navigation on the pricing
table, wired to the existing `recdrawer`.

**Structure, Simulator, Glossary** - untouched apart from the global em dash sweep and the
meta guards.

**Shell and TRACE module** - meta guards, em dash sweep, external references removed, the
automatic service probe removed.

## 7. Bulk administrator marks: how it reaches the engine

The obvious seam, `reviseApplyNav()`, is **broken for the embedded dataset** and was
rejected. `buildBaseStruct()` reads `n.holder` from `EMB.nodes`, and those nodes carry no
`holder` property, so it builds a structure keyed on `undefined`. Measured: calling
`reviseApplyNav(currentNavMap())` with **no overrides at all** moves Revised MV from
2,060,610,338.29 to 2,062,198,835.86, a $1.59m change. Evidence in `tools/probe_calc3.js`.

Instead the panel assembles the struct from the look-through tree's **own path parentage,
its existing `ownpct` and its existing `mv100`**, and hands it to the app's own
`recomputeR()`. Nothing is recomputed or re-derived in new code. Proof that the seam is
correct: at zero overrides this reproduces the live engine **exactly** - all 26 funds
match, product Revised MV difference is 0. That equality is itself a test
(`bulkupload.test.js`, second case).

Per-SPV deltas are produced by pricing each accepted row **on its own through the same
engine**, so they are engine output rather than a formula. Because `recomputeR` is affine
in the overridden values, the per-SPV deltas sum exactly to the combined delta - which is
what the 12-row test asserts, to the cent, for both product NAV and Revised MV.

Rejection reasons: unknown SPV code, duplicate code, no value supplied, non-numeric value,
both market value and NAV supplied. Matching is **exact after trimming** - no case folding
and no fuzzy matching, so `SPORTB_TYPO` is rejected rather than coerced to `SPORTB`
(asserted in `rejected.test.js`).

## 8. Done item status

| # | Item | State | Proven by |
| --- | --- | --- | --- |
| 1 | Figure snapshot byte-identical | **pass** | `COMPARE OK`, 1458 figures, sha256 `dee02640...` |
| 2 | One subtotal row per group, foots to the cent | **pass** | `subtotal.test.js` - 44 group subtotals |
| 3 | Left rail, 5+ levels, `data-level` | **pass** | `hierarchy.test.js` - levels 0..7, 8 distinct colours |
| 4 | Hierarchy + NAV pinned at max scrollLeft | **pass** | `sticky.test.js` |
| 5 | Bulk CSV override, per-SPV delta, product NAV before/after | **pass** | `bulkupload.test.js` - 12 rows, deltas foot to the cent |
| 6 | Rejected rows with reasons, never silently dropped | **pass** | `rejected.test.js` - 3 unmatched, 0 reached the calculation |
| 7 | 5-segment waterfall, delta Pricing filters the table | **pass** | `waterfall.test.js` - 149 rows filtered to 24 contributors |
| 8 | Arrow keys, Enter, Escape in both tables | **pass** | `keyboard.test.js` |
| 9 | Playwright gate, zero console errors | **pass** | 6 console errors -> 0; 5 network requests -> 0 |
| 10 | Self-contained, no external requests | **pass** | 9 external references -> 0 |
| 11 | Five iOS light-only meta guards | **pass** | 0/15 -> 15/15 |
| 12 | Zero em dashes | **pass** | 332 -> 0 |
| 13 | No baseline test regresses | **pass** | Baseline had no passing tests; all 10 now pass |

### Baseline fail set, for the record

Before any change: snapshot n/a (it was the baseline), no test suite existed,
`playwright_gate` **failed** (6 console errors, 6 failed requests, 5 network requests),
`check_selfcontained` **failed** (9 external references, 0/15 guards, 332 em dashes).

## 9. Bugs found and fixed during the work

1. **Pricing rows were not focusable.** The roving tabindex only ran over *visible* rows,
   and a table on a hidden tab has no `offsetParent`, so the pricing table never became
   keyboard reachable. Fixed by assigning tabindex to all rows regardless of visibility.
2. **Fixed-width identity column overflowed into the VPM Symbol column.** A `max-width` on
   a table cell is only a hint. Replaced with natural sizing plus a measured sticky offset.
3. **`text-overflow:ellipsis` on the cell blanked long fund names.** `.nm` is an
   inline-block, and an inline-block that does not fit is dropped whole rather than
   clipped. Removed from the cell; `.nm` carries its own ellipsis and is capped instead.
4. Two snapshot-tool corrections, both described in section 5.

## 10. Dead ends, with the evidence that ruled them out

- **Vendoring d3/xlsx** to satisfy "no external requests": impossible here, cdnjs returns
  proxy 403.
- **`reviseApplyNav()` as the override seam**: destructive for the embedded dataset, moves
  Revised MV by $1.59m with zero overrides. See section 7.
- **`reviseModel(MAPS, ...)` as the override seam**: `MAPS` is `null` until a Position
  Report is uploaded, so it is unavailable in the shipped state.
- **A closing subtotal row per group**: adds rendered figures, incompatible with item 1.
  Escalated as B4 and resolved by the user.

## 11. Standing assumptions

- The figure snapshot is taken with all external requests blocked, matching the stated
  deployment mode (double-click, offline). Allowing real network access would make the
  baseline depend on CDN availability, which the brief lists as an immediate halt
  condition.
- `MOD_TRACE` still contains an **opt-in** service integration (`TRACE.setService(url)` /
  `?svc=`). It fires no request unless a user explicitly configures a service, which is
  why the runtime gate measures zero network requests. It was left in place because the
  TRACE module is outside this project's scope.


---

# Cycle 1 - position-variance exceptions

Ran as a separate improvement cycle on top of Phase 1. Full decision record in
`cycles/1.md`; this section is the operational log.

## Branch note

The cycle brief asks for a new branch off main. The environment's git policy pins all
development and pushes to `claude/trace-pro-readability-overrides-j32s9z`, and branching
off main would also discard the Phase 1 work this cycle builds on. The cycle therefore ran
on the designated branch as a separate commit with its own cycle record. Logged rather than
silently deviated from.

## Missing cycle input

`friction-log.md` does not exist in the repo and `cycles/` was empty before this run. Of the
three candidate sources the Ranking Rule names, only two were available: harness outliers
and failures recorded in the Phase 1 ledger. No candidate was invented to fill the gap, and
the limitation is recorded in `cycles/1.md` section 2. Writing the friction log was itself
ranked as candidate C6 (tier 3) and lost.

## Baseline

Pre-cycle app frozen at `baseline/precycle_TRACE_Platform.html`
(sha256 `fa3f212b60040950b7bc9a3a9e6b437a1e35b66f8a2300a3ba1cd921b7c72c5c`), so the baseline
stays reproducible after dist changes.

Baseline gate, all green before any change: snapshot OK (1458 figures), 10/10 tests,
playwright pass, self-contained pass.

Two baseline harness runs, `baseline/harness.json`:

| task | run 1 clicks | run 2 clicks | run 1 s | run 2 s | seconds drift |
| --- | ---: | ---: | ---: | ---: | ---: |
| T1 | 13 | 13 | 1.23 | 1.24 | 0.8% |
| T2 | 1 | 1 | 0.13 | 0.13 | 0.0% |
| T3 | 39 | 39 | 4.70 | 4.68 | 0.4% |
| T4 | 2 | 2 | 0.17 | 0.17 | 0.0% |
| T5 | 6 | 6 | 1.43 | 1.43 | 0.0% |

Clicks identical, seconds within 0.8 percent: repeatable.

## Iterations

1. Built `tools/task_harness.js`. First run: T1, T3, T5 produced wrong answers.
   Root causes, all in the harness rather than the app: rows carry no `data-code` (fixed by
   reading the on-screen `.codetag`), the T3 loop dismissed the drawer with Escape which
   re-rendered the tree and reset the scroll, and T5 guessed selectors for a scroll
   container and a button that do not exist (the ownership table rides the page; the
   control is `#rollupmore`).
2. T3 still found only 17 of 26 SPVs. Root cause: the scroll step advanced by the
   container's `clientHeight` (654px) while only 193px of it was on screen, skipping rows.
   Fixed by scrolling by the **visible** height and adding `bringIntoView`, which models a
   controller scrolling the page to give the table the screen before scanning it.
3. Baseline recorded and confirmed repeatable. Ranking table written to `cycles/1.md`
   before any application code was touched. Chose C1, the top-ranked entry.
4. Built C1 as an isolated asset pair (`assets/cycle1-variance.{css,js}`) injected by
   `tools/patch.js`, so the cycle is a self-contained, revertible diff.
5. Harness fast path read `[null]`: `data-var-code` was being stored under the key
   `var-code`. Fixed the harness's attribute conversion, then **re-ran the baseline** to
   confirm the fix did not move it. It did not.
6. Panel read 16 of 20 rows: `#ltvarwrap` was the outer div, which does not scroll.
   Restructured so the id sits on the element that actually scrolls.
7. Sticky-column test regressed: "pinned hierarchy and NAV columns overlap". Real
   regression, not a flake. Root cause: the variance marker widens the identity cell after
   Phase 1 has already measured `--hier-w`. Fixed by exporting `syncFrozenOffset` and
   re-running it at the end of the cycle 1 decorator, so the offset is taken after the last
   mutation rather than the first.
8. Escape test failed. Verified the handler directly first: a synthetic Escape on document
   and a real Escape on the chip both close the panel. The test was pressing on `body`,
   which is not focusable, so the key never reached the document. Test fixed to press where
   focus actually is; behaviour was never broken.

## Result

| task | before | after | change |
| --- | ---: | ---: | --- |
| T1 | 13 | 13 | unchanged |
| T2 | 1 | 1 | unchanged |
| **T3** | **39** | **4** | **-89.7% clicks, -90.9% seconds** |
| T4 | 2 | 2 | unchanged |
| T5 | 6 | 6 | unchanged |

Gate: harness compare PASS, snapshot byte-identical (1458 figures, sha256
`dee026405a26d84ceb37d28eef249f10e3e2406d5f2913bfd32eea65d982cce0`), 15/15 tests
(10 pre-existing + 5 new), playwright 0 console errors, self-contained pass.

## Dead ends this cycle

- Counting a scroll step as free: makes every task cost one or two clicks and no
  improvement measurable, because a headless browser reads the whole DOM at once and a
  controller cannot. Rejected in favour of the viewport reading rule.
- Scrolling by the container's `clientHeight`: silently skipped rows and under-reported
  the baseline cost. Replaced with the visible-height step.
- Putting a breach count on the chip: a digit there is a new rendered figure in the default
  view and would fail the snapshot. The chip carries no number; the count lives in the
  panel, which renders nothing until opened.

## Current hypothesis for the next cycle

C2, the broken `reviseApplyNav()` NAV-Report path, is the highest-value remaining item and
the only known way a controller can be shown a wrong number today. It needs an explicit
decision to open the calculation engine before it can be worked.
